void downloadBackupOnWeb(String data, String filename) {
  // Diese Funktion wird für mobile Plattformen nicht verwendet
  throw UnsupportedError('Direct web download is not supported on mobile platforms');
}