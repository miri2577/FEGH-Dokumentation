void downloadFileOnWeb(String content, String fileName) {
  // Diese Funktion wird nur für mobile Plattformen verwendet
  // Der eigentliche Download wird über FileSaver gehandhabt
  throw UnsupportedError('Direct web download is not supported on mobile platforms');
}